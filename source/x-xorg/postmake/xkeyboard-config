if [ -d ${packageDir}/usr/share/pkgconfig ]; then
  mkdir -p ${packageDir}/usr/lib${libSuffix}
  mv ${packageDir}/usr/share/pkgconfig ${packageDir}/usr/lib${libSuffix}/
  rmdir ${packageDir}/usr/share 2>/dev/null || true;
fi

