if [ -d ${packageDir}/usr/lib${libSuffix}/X11/xedit ]; then
  (
    mkdir -p ${packageDir}/etc/X11
    cd ${packageDir}/usr/lib${libSuffix}/X11
    mv xedit ${packageDir}/etc/X11
    ln -sf ../../../etc/X11/xedit .
  )
fi
