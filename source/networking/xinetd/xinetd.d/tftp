# Begin /etc/xinetd.d/tftp

service tftp
{
   disable        = yes
   socket_type    = dgram
   wait           = yes
   user           = 0
   server         = /usr/sbin/in.tftpd
}

# End /etc/xinetd.d/tftp
