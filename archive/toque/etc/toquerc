#
#                 Runtime Configuration file for Toque
#
# Toque looks for this file in:
#	1 - $HOME/.toquerc
#	2 - ${sysconfdir}/toquerc

# FIRST SECTION: Default options
# Uncomment each option you want set.
# The command line options override those specified in this file.

#	--keep                  keep (don't delete) srcdir

#	--outdir=/tmp/packages  directory where packages are written

#	--quiet                 suppress all messages

#	--strip=sections        types of symbols to be stripped from object files

#	--tmpdir=/tmp/sources   temporary directory for compilation

#	--verbose               be verbose (a 2nd -v gives more)


# SECOND SECTION: Definitions of parameters.
# Whitespace around the parameter name and value is ignored.
# Value can be a list of words with embedded spaces.
# Syntax: <name> = <value>

	configure_args = --prefix=/usr \
                         --libexecdir=/usr/libexec \
                         --bindir=/usr/bin \
                         --sbindir=/usr/sbin \
                         --sysconfdir=/etc \
                         --includedir=/usr/include \
                         --localstatedir=/var \
                         --mandir=/usr/share/man \
                         --infodir=/usr/share/info

# DRAGORA SECTION: Specific definitions for Dragora

archive = /usr/src/dragora/archive
patchdir = /usr/src/dragora/patches
tardir = /usr/src/dragora/sources
