#
# Cross toolchain variables for the environment
#

CC="${TARGET}-gcc"
CXX="${TARGET}-g++"
AR="${TARGET}-ar"
AS="${TARGET}-as"
LD="${TARGET}-ld"
RANLIB="${TARGET}-ranlib"
READELF="${TARGET}-readelf"
STRIP="${TARGET}-strip"

export CC CXX AR AS LD RANLIB READELF STRIP

