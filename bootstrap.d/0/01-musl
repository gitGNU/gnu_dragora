# Build script for musl (headers)
#
# Copyright (C) 2014, 2015 Matias A. Fonzo.
#
# This script is free software: you have unlimited permission
# to copy, distribute and modify it.

version=1.1.10

echo "Unpacking musl-${version} ..."
rm -rf "${tmpdir}"/musl-${version}
tar xf "${tardir}"/musl-${version}.tar.lz -C "$tmpdir"

# Build instructions
cd "${tmpdir}"/musl-${version}

make install-headers prefix="${crossdir}"/$target ARCH="$MUSL_ARCH"

