#
# qirc: Configuration file for qi(1).
#
# Uncomment a variable to set a new value other than default.
#
# For more information, type: info qi 'the qirc file'
#

## FIRST SECTION:  Package Settings.
#
# Package installation directory
#packagedir=/usr/packages

# Target directory for links
#targetdir=/

## SECOND SECTION:  Build Settings.
#
# Output directory where the packages are written
#outdir=/var/cache/qi/packages

# Temporary directory for compilation
#tmpdir=/tmp/sources

# Working directory tree for archive, patches, recipes
#worktree=/usr/src/qi

# Where to find the compressed sources
#tardir=/usr/src/qi/sources

# Strip symbols from object files ['strip' or 'nostrip']
#strip=strip

# Architecture to use (autodetected)
#arch=

# Parallel jobs for the compiler
#jobs=1

# General download manager
#netget="wget --continue --wait=1 --tries=3 --no-check-certificate --passive-ftp"

# Download manager for the RSYNC protocol
#rsync="rsync --verbose --archive --copy-links --compress --progress --itemize-changes"

# Compress Info documents and Manual pages.
#
# The default is to compress documents unless 'nocompress_pages' is used.
#compress_pages=compress_pages

# Default prefixes for Info documents and Manual pages.
#
# Make sure to point the same values for configure_args=.
#infodir=/usr/share/info
#mandir=/usr/share/man

# Arguments for 'configure'
configure_args=--prefix=/usr --libexecdir=/usr/libexec --bindir=/usr/bin --sbindir=/usr/sbin --sysconfdir=/etc --localstatedir=/var --infodir=/usr/share/info --mandir=/usr/share/man
